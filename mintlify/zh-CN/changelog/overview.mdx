---
title: "更新日志"
description: "Forge 的最新变化、功能更新与改进说明,帮助你及时掌握验证、会话与命令行工具的所有关键升级信息。"
---

<Update label="July 19, 2026">

## 更新内容

**`forge verify --deep` 现在返回四态结果。** 多视角共识运行不再折叠为二元的通过/失败结果。你将看到 `PASS`、`FAIL`、`INCOMPLETE` 或 `NOT_CONFIGURED` 中的一种,因此停滞的视角或未配置的仓库不会再被悄悄标记为绿色。只有 `PASS` 才算作已验证,而 `PASS` 现在还要求底层 `forge verify` 的测试状态也为 `PASS`——绿色的深度运行永远无法凌驾于红色的基础运行之上。

参见[质量命令](/zh-CN/cli/quality)和[验证门](/zh-CN/concepts/verification-gates)。

**完成门要求代码变更提供测试证据。** 对于任何触及源代码的会话,Stop 路径上的完成门不再单独接受 `forge handoff` 快照。你需要在会话的 diff 中包含一个测试文件,或针对当前变更提供一次新鲜通过的 `forge verify`。仅涉及文档、仅涉及配置以及其他非代码类会话不受影响。

当此门拦截时,修复检查清单现在会以 `forge verify` 作为首要步骤,以便在生成 handoff 快照之前先记录证据。参见[跨会话记忆](/zh-CN/concepts/cross-session-memory)。

</Update>
