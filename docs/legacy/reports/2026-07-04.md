# Daily Config Radar — 2026-07-04

**TL;DR**
- 🔺 **Claude Code 2.1.200** (Jul 3) changes the *default permission mode to "Manual"* and stops AskUserQuestion auto-continuing — your `settings.json` uses `defaultMode: "default"`, worth reviewing. Subagents now run in the background by default (helps your `scout`/`verifier` flow).
- 🎨 **shadcn/ui** made **Base UI the default** (over Radix) as of July 2026 — your `ui-workflow` skill + shadcn MCP should expect Base UI in new projects.
- 🔴 **Unrotated secret still open**: the GitHub PAT flagged in `RECONCILE.md` / `RUN.md` needs rotating — this is the top hygiene item and it's not code-fixable by the task.

---

## 1. Stay-current / Dev-radar

**Claude Code — v2.1.200 (2026-07-03)**
- Default permission mode changed from "default" to **"Manual"** across CLI, VS Code, JetBrains. Your `global/settings.json` sets `permissions.defaultMode: "default"` — decide whether to move to `"Manual"` to match upstream, or keep your pre-allowed list as the intentional override.
- **AskUserQuestion no longer auto-continues** by default (opt-in via `/config`).
- **Subagents now run in the background by default** — Claude keeps working while `scout`/`verifier` run and notifies on finish. Net win for your delegation-heavy setup; no config change needed.
- New **`/dataviz` skill** for chart/dashboard design with a color-palette validator.
- Fixes: startup crash when `disabledMcpServers`/`enabledMcpServers` is a non-array; background sessions silently stopping after sleep/wake.
- Source: [Claude Code changelog](https://code.claude.com/docs/en/changelog) · [Releasebot – Claude Code](https://releasebot.io/updates/anthropic/claude-code)

**shadcn/ui — Base UI is now the default (July 2026)**
- `npx shadcn init` / `shadcn/create` now pick **Base UI** over Radix (~2:1); docs default to the Base UI tab. Radix is **not** deprecated — components still ship for both.
- Base UI stable at 1.6.0. Impact: your `ui-workflow` skill and shadcn MCP will scaffold Base UI by default in new work — sanity-check any Radix-specific assumptions.
- Also recent (June): new chat components (MessageScroller, Message, Bubble, Attachment, Marker).
- Source: [shadcn/ui – Base UI as Default](https://ui.shadcn.com/docs/changelog/2026-07-base-ui-default) · [shadcn/ui changelog](https://ui.shadcn.com/docs/changelog)

*Context7, OpenRouter, Cloudflare, pgvector: no material 24h changes surfaced this run.*

---

## 2. Config drift / hygiene

- 🔴 **Rotate the GitHub PAT.** `RECONCILE.md` and `RUN.md` both flag a literal `github_pat_…` in `~/.claude/settings.json` → `mcpServers.github.env`. Action: revoke at github.com/settings/tokens, reissue fine-scoped, reference as `${GITHUB_PAT}` from shell env. (Value not printed here.)
- 🔴 **Rotate the my-next-app WHMCS creds.** `PLAYBOOK.md` notes a committed `.env` with WHMCS creds. Action: `git rm --cached .env`, gitignore it, rotate.
- 🟡 **Permission mode vs upstream.** `global/settings.json` → `permissions.defaultMode: "default"`; upstream default is now "Manual" (see §1). Reconcile intentionally.
- 🟡 **Learned-lessons consolidation.** `RESEARCH-claude-config.md` gap #2: the session-learning hook only *appends* to `~/.claude/skills/learned/` — schedule a weekly dedupe/prune pass before it bloats context.
- 🟡 **Plugin/token audit outstanding.** `RESEARCH-claude-config.md` gap #1: ~21 enabled plugins, none token-audited. Action: `claude plugin details <name>` per plugin; disable low-value ones (firecrawl, postman, data-engineering, atomic-agents, microsoft-docs, greptile, playground, sentry, gitlab).
- 🟢 Bundle `settings.json` is clean: `model: sonnet`, secret-file `deny` rules and destructive-command denies in place, three hooks + statusline wired.

---

## 3. Repost candidates

1. **"shadcn/ui just made Base UI the default over Radix."** Base UI hits 1.6.0 / 6M+ weekly downloads; new projects now pick it 2:1. Radix isn't going away — components ship for both. [link](https://ui.shadcn.com/docs/changelog/2026-07-base-ui-default)
2. **"Claude Code 2.1.200 flips the default permission mode to Manual + runs subagents in the background."** If you lean on subagents, they now work while you keep going. [link](https://code.claude.com/docs/en/changelog)
3. *(Config tip, evergreen)* **"The cheapest Claude Code optimization isn't a new plugin — it's rotating the token you left in settings.json and auditing the 21 plugins you never use."** Straight from this week's config reconcile.

---
*Generated by the `daily-claude-config-radar` scheduled task. Next run: tomorrow ~6am.*
