# Claude Config Radar — 2026-07-05

**TL;DR**
- 🔴 **Security-relevant dep bump**: pgvector **0.8.2** fixes a buffer overflow in parallel HNSW index builds (**CVE-2026-3172**) — your stack uses pgvector, so this is the one worth acting on.
- 🟡 **shadcn/ui** made **Base UI the default** component library (docs + new projects) as of July 2026 — escalation of yesterday's "picking Base UI ~2:1" note; Radix not deprecated.
- 🔴 **Unrotated secrets still open** (GitHub PAT + my-next-app WHMCS creds) per legacy docs — not code-fixable, still needs you. No change since 2026-07-04.

---

## 1. Stay-Current / Dev-Radar

Diffed against the prior run (`docs/legacy/reports/2026-07-04.md`). New/material only:

- **pgvector 0.8.2 — security fix (NEW, act on this).** Fixes a buffer overflow with parallel HNSW index builds, tracked as CVE-2026-3172. Rolling out via managed PG (e.g. RDS PG 18.4). Since Forge's stack notes list Postgres+pgvector for RAG, bump when your managed provider offers it. [postgresql.org](https://www.postgresql.org/about/news/pgvector-082-released-3245/) · [pgvector/pgvector](https://github.com/pgvector/pgvector)
- **shadcn/ui — Base UI is now THE default (escalation).** Docs open on the Base UI tab and `shadcn init` scaffolds Base UI for new projects. Radix is *not* deprecated and still ships for both; add `-b radix` in non-interactive CI to pin Radix. Yesterday flagged the trend; it's now the formal default. [ui.shadcn.com/changelog/2026-07-base-ui-default](https://ui.shadcn.com/docs/changelog/2026-07-base-ui-default)
- **Claude Code v2.1.201 (Jul 3–4).** Sonnet 5 sessions no longer use the mid-conversation system role for harness reminders. Builds on v2.1.200's **Manual default permission mode** and AskUserQuestion no longer auto-continuing (v2.1.200 was largely covered yesterday). [code.claude.com/docs/changelog](https://code.claude.com/docs/en/changelog)
- **Cloudflare `@cloudflare/workers-types` v5 (Jul 3).** Dated entrypoints (e.g. `/2023-03-01`) removed; generate types locked to your compat date via `wrangler types`. Relevant to any Workers deploy targets in the project layer. [developers.cloudflare.com/changelog/…workers-types-v5](https://developers.cloudflare.com/changelog/post/2026-07-03-workers-types-v5/)
- **OpenRouter — nothing material in the window.** The real model wave was late May–June (Opus 4.8, Gemini 3.5 Flash, Model Fusion). July is just free-tier count churn (25→26 models). No action. [openrouter.ai/announcements](https://openrouter.ai/announcements)
- **Context7 — no material change surfaced.** Docs-retrieval tool, low churn; nothing notable in the last day.

## 2. Config Drift / Hygiene

- 🔴 **Rotate the GitHub PAT.** Flagged in `docs/legacy/RECONCILE.md:7` and `docs/legacy/RUN.md:22–25` (literal `github_pat_…` in `~/.claude/settings.json` → `mcpServers.github.env`). Fix: revoke at github.com/settings/tokens, reissue fine-scoped, reference as `${GITHUB_PAT}` from shell env. Note: the *active* bundle now ships `global/settings.template.json` (no literal token in-repo), so verify what's actually in your live `~/.claude/settings.json`. (Value not printed.)
- 🔴 **Rotate the my-next-app WHMCS creds.** `docs/legacy/PLAYBOOK.md:45–46` and `docs/legacy/RUN.md:27–28` note a committed `.env`. Fix: `git rm --cached .env`, gitignore it, rotate the creds. Unchanged since 2026-07-04.
- 🟡 **Report location drift.** The task expects `reports/` at repo root, but the prior run wrote to `docs/legacy/reports/2026-07-04.md`. This run created root `reports/` and wrote here. Pick one home and move the 07-04 file so history doesn't fragment. Fix: `git mv docs/legacy/reports/2026-07-04.md reports/`.
- 🟡 **Forge refactor left legacy docs behind.** `.remember/today-2026-07-05.md` records today's "Forge ecosystem" ship (11 commits, 36/36 tests); `RUN.md`/`PLAYBOOK.md`/`RECONCILE.md` are now under `docs/legacy/` and still reference the old `~/.claude` layout. Active config is `global/CLAUDE.md` + `global/settings.template.json` + `guards`/`crew`/`recall`. Fix: add a one-line banner atop each legacy doc pointing to the current equivalent (or fold rotation TODOs into a live `SECURITY.md`) so the secret flags don't rot in an archive.
- 🟢 **No newly archived/broken skills.** `global/rules/stack-notes.md:9` correctly self-flags its stack list as potentially stale and defers to `tech-selector`/`dev-radar` — healthy. No disabled/deprecated skills detected in the active `global/` tree.

## 3. Repost Candidates

1. **pgvector 0.8.2** — "pgvector 0.8.2 is out and it's a security patch, not a feature drop: fixes a buffer overflow in parallel HNSW index builds (CVE-2026-3172). If you run vector search on Postgres, bump it."
2. **shadcn/ui → Base UI default** — "shadcn/ui just made Base UI the default. New projects scaffold on it, docs default to it — and Radix isn't going anywhere (`-b radix` still pins it). Progressive migration, no forced rewrite."
3. **Claude Code Manual permission mode** — "Claude Code's default permission mode is now Manual, and AskUserQuestion no longer auto-continues. Fewer surprise actions, more explicit approval. Worth a look if you run it unattended."

<!-- Generated by daily-claude-config-radar. Sources cited inline. Secrets never printed. -->
